---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const title = "Calculadora de Juros Compostos";
const description = "Simule o crescimento do seu investimento com juros compostos. Veja quanto seu dinheiro vai render com aportes mensais, taxa de juros e prazo personalizados.";
const canonicalUrl = new URL('/calculadora-juros-compostos/', Astro.site).href;
---

<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <title>{title} — Negócio Certo</title>
  <meta name="description" content={description} />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href={canonicalUrl} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={canonicalUrl} />
  <link rel="preconnect" href="https://cdnjs.cloudflare.com" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
</head>
<body>
  <Header />

  <!-- HERO -->
  <section class="calc-hero">
    <div class="calc-hero-inner">
      <nav aria-label="Breadcrumb" class="calc-breadcrumb">
        <ol>
          <li><a href="/">Home</a></li>
          <li><span aria-hidden="true">›</span></li>
          <li><a href="/calculadoras/">Calculadoras</a></li>
          <li><span aria-hidden="true">›</span></li>
          <li><span class="current">Calculadora de Juros Compostos</span></li>
        </ol>
      </nav>
      <h1 class="calc-hero-title">Calculadora de Juros Compostos</h1>
      <p class="calc-hero-desc">Descubra quanto seu dinheiro pode render ao longo do tempo com o poder dos juros compostos.</p>
    </div>
  </section>

  <!-- FERRAMENTA -->
  <main class="calc-main">
    <div class="calc-tool">

      <!-- PAINEL ESQUERDO: FORM -->
      <aside class="calc-form-panel">
        <h2 class="calc-form-title">Detalhes do investimento</h2>

        <div class="calc-field">
          <label class="calc-label" for="inp-inicial">
            Depósito inicial
            <span class="tip-wrap" aria-label="Valor que você já possui para investir agora. É o ponto de partida da simulação.">
              <span class="tip-icon" aria-hidden="true">i</span>
              <span class="tip-box" role="tooltip">Valor que você já possui para investir agora. É o ponto de partida da simulação.</span>
            </span>
          </label>
          <div class="calc-input-wrap">
            <span class="calc-unit left">R$</span>
            <input id="inp-inicial" class="calc-input" type="text" inputmode="numeric" value="10.000" />
          </div>
        </div>

        <div class="calc-field">
          <label class="calc-label" for="inp-anos">
            Anos de crescimento
            <span class="tip-wrap" aria-label="Por quantos anos você pretende deixar o dinheiro investido.">
              <span class="tip-icon" aria-hidden="true">i</span>
              <span class="tip-box" role="tooltip">Por quantos anos você pretende deixar o dinheiro investido.</span>
            </span>
          </label>
          <div class="calc-input-wrap">
            <input id="inp-anos" class="calc-input" type="text" inputmode="numeric" value="10" />
            <span class="calc-unit right">Anos</span>
          </div>
        </div>

        <div class="calc-field">
          <label class="calc-label" for="inp-taxa">
            Taxa de retorno estimada
            <span class="tip-wrap" aria-label="Taxa de juros anual do investimento (% a.a.). Por exemplo: 12% significa que o dinheiro rende 12% ao ano.">
              <span class="tip-icon" aria-hidden="true">i</span>
              <span class="tip-box" role="tooltip">Taxa de juros anual do investimento (% a.a.). Por exemplo: 12% significa que o dinheiro rende 12% ao ano.</span>
            </span>
          </label>
          <div class="calc-input-wrap">
            <input id="inp-taxa" class="calc-input" type="text" inputmode="decimal" value="12%" />
          </div>
        </div>

        <div class="calc-field">
          <label class="calc-label" for="inp-freq-cap">
            Frequência de capitalização
            <span class="tip-wrap" aria-label="De quanto em quanto tempo os juros são calculados e somados ao saldo. Capitalização mensal é o padrão da maioria dos investimentos brasileiros (CDB, Selic, poupança).">
              <span class="tip-icon" aria-hidden="true">i</span>
              <span class="tip-box" role="tooltip">De quanto em quanto tempo os juros são calculados e somados ao saldo. Capitalização mensal é o padrão da maioria dos investimentos brasileiros (CDB, Selic, poupança).</span>
            </span>
          </label>
          <div class="calc-select-wrap">
            <select id="inp-freq-cap" class="calc-select">
              <option value="12" selected>Mensal</option>
              <option value="4">Trimestral</option>
              <option value="2">Semestral</option>
              <option value="1">Anual</option>
              <option value="365">Diária</option>
            </select>
            <span class="calc-select-arrow" aria-hidden="true">▼</span>
          </div>
        </div>

        <div class="calc-field">
          <label class="calc-label" for="inp-aporte">
            Valor do aporte
            <span class="tip-wrap" aria-label="Quanto você pretende depositar periodicamente, além do valor inicial. Aportes regulares aceleram muito o crescimento do investimento.">
              <span class="tip-icon" aria-hidden="true">i</span>
              <span class="tip-box" role="tooltip">Quanto você pretende depositar periodicamente, além do valor inicial. Aportes regulares aceleram muito o crescimento do investimento.</span>
            </span>
          </label>
          <div class="calc-input-wrap">
            <span class="calc-unit left">R$</span>
            <input id="inp-aporte" class="calc-input" type="text" inputmode="numeric" value="500" />
          </div>
        </div>

        <div class="calc-field">
          <label class="calc-label">
            Frequência do aporte
            <span class="tip-wrap" aria-label="Se você vai depositar o valor do aporte todo mês ou uma única vez por ano. Aportes mensais tendem a gerar mais rendimento por aproveitar melhor os juros compostos.">
              <span class="tip-icon" aria-hidden="true">i</span>
              <span class="tip-box" role="tooltip">Se você vai depositar o valor do aporte todo mês ou uma única vez por ano. Aportes mensais tendem a gerar mais rendimento por aproveitar melhor os juros compostos.</span>
            </span>
          </label>
          <div class="calc-toggle-group" role="group" aria-label="Frequência do aporte">
            <button class="calc-toggle active" data-value="monthly" type="button">Mensal</button>
            <button class="calc-toggle" data-value="annually" type="button">Anual</button>
          </div>
        </div>
      </aside>

      <!-- PAINEL DIREITO: GRÁFICO -->
      <div class="calc-chart-panel">
        <div class="calc-chart-header">
          <p class="calc-chart-label">Saldo Total</p>
          <p class="calc-chart-total" id="result-total">R$ 231.865,21</p>
        </div>
        <div class="calc-chart-area">
          <canvas id="grafico" aria-label="Gráfico de crescimento do investimento"></canvas>
        </div>
        <div class="calc-chart-legend">
          <span class="calc-legend-item">
            <span class="calc-legend-swatch principal"></span>
            Principal total
          </span>
          <span class="calc-legend-item">
            <span class="calc-legend-swatch juros"></span>
            Juros acumulados
          </span>
        </div>
      </div>

    </div>

    <!-- TABELA ANO A ANO -->
    <div class="calc-table-section">
      <h2 class="calc-section-title">Evolução ano a ano</h2>
      <div class="calc-table-scroll">
        <table class="calc-table">
          <thead>
            <tr>
              <th>Ano</th>
              <th>Saldo Inicial</th>
              <th>Aportes no Ano</th>
              <th>Juros no Ano</th>
              <th>Saldo Final</th>
            </tr>
          </thead>
          <tbody id="tabela-body">
            <!-- preenchido pelo JavaScript -->
          </tbody>
        </table>
      </div>
    </div>

    <!-- EXPLICAÇÃO -->
    <div class="calc-info">
      <div class="calc-info-text">
        <h2>O que são juros compostos?</h2>
        <p>Juros compostos são calculados sobre o capital inicial somado aos juros já acumulados — é o chamado <strong>"juros sobre juros"</strong>. Quanto mais tempo o dinheiro fica investido, mais rápido ele cresce de forma exponencial.</p>
        <h3>Fórmula utilizada</h3>
        <div class="calc-formula-block">
          <code>M = P × (1 + i/n)<sup>n×t</sup> + C × [(1 + i/n)<sup>n×t</sup> − 1] / (i/n)</code>
        </div>
        <div class="calc-formula-vars">
          <div><strong>M</strong> — Montante final</div>
          <div><strong>P</strong> — Capital inicial</div>
          <div><strong>i</strong> — Taxa de juros anual</div>
          <div><strong>n</strong> — Frequência de capitalização/ano</div>
          <div><strong>t</strong> — Prazo em anos</div>
          <div><strong>C</strong> — Aporte periódico</div>
        </div>
      </div>
    </div>

  </main>

  <Footer />

  <script is:inline>
  (function () {
    'use strict';

    var chart = null;
    var debounceTimer = null;

    var BRL = new Intl.NumberFormat('pt-BR', {
      style: 'currency', currency: 'BRL', minimumFractionDigits: 2, maximumFractionDigits: 2
    });

    /* ---------- helpers ---------- */

    function parseVal(str) {
      var s = String(str).replace(/[R$\s%]/g, '').trim();
      // Se tem vírgula: ponto = milhar, vírgula = decimal  ("1.500,50")
      if (s.indexOf(',') !== -1) {
        s = s.replace(/\./g, '').replace(',', '.');
      } else {
        // Remove ponto usado como separador de milhar ("10.000")
        // Heurística: ponto seguido de exatamente 3 dígitos (e depois outro ponto ou fim)
        s = s.replace(/\.(\d{3})(?=\.|$)/g, '$1');
      }
      return parseFloat(s) || 0;
    }

    function fmtK(v) {
      if (v === 0) return 'R$ 0';
      if (v >= 1000000) return 'R$ ' + (v / 1000000).toFixed(1).replace(/\.0$/, '') + 'M';
      if (v >= 1000)    return 'R$ ' + (v / 1000).toFixed(1).replace(/\.0$/, '') + 'k';
      return BRL.format(v);
    }

    /* ---------- cálculo ----------
     * Simula período a período para qualquer frequência de capitalização.
     * Para aportes mensais: distribui proporcionalmente por período de capitalização.
     * Para aportes anuais: aplica no início de cada ano.
     */
    function calcularAnos(P, taxaAnual, qtdAnos, aporte, freqAporte, freqCap) {
      var r    = taxaAnual / 100;
      var rPer = r / freqCap;          // taxa por período de capitalização
      var saldo = P;
      var rows  = [];

      for (var ano = 1; ano <= qtdAnos; ano++) {
        var saldoInicio   = saldo;
        var aporteNoAno   = (freqAporte === 'monthly') ? aporte * 12 : aporte;

        for (var per = 0; per < freqCap; per++) {
          // Adiciona aporte no início de cada período proporcional
          if (freqAporte === 'monthly') {
            saldo += aporte * (12 / freqCap);
          } else {
            if (per === 0) saldo += aporte;
          }
          saldo *= (1 + rPer);
        }

        var juroNoAno = saldo - saldoInicio - aporteNoAno;

        rows.push({
          ano:          ano,
          saldoInicio:  saldoInicio,
          aporteNoAno:  aporteNoAno,
          juroNoAno:    juroNoAno,
          saldoFinal:   saldo,
        });
      }

      return rows;
    }

    /* ---------- leitura dos inputs ---------- */
    function lerInputs() {
      return {
        P:          parseVal(document.getElementById('inp-inicial').value),
        anos:       Math.max(1, Math.min(50, Math.round(parseVal(document.getElementById('inp-anos').value)))),
        taxa:       parseVal(document.getElementById('inp-taxa').value),
        freqCap:    parseInt(document.getElementById('inp-freq-cap').value) || 12,
        aporte:     parseVal(document.getElementById('inp-aporte').value),
        freqAporte: (document.querySelector('.calc-toggle.active') || {}).dataset?.value || 'monthly',
      };
    }

    /* ---------- atualiza UI ---------- */
    function atualizar() {
      var inp  = lerInputs();
      var rows = calcularAnos(inp.P, inp.taxa, inp.anos, inp.aporte, inp.freqAporte, inp.freqCap);
      var final = rows.length ? rows[rows.length - 1].saldoFinal : inp.P;

      // Total em destaque
      document.getElementById('result-total').textContent = BRL.format(final);

      // --- Gráfico ---
      var anoAtual    = new Date().getFullYear();
      var labels      = ['Agora'];
      var dPrincipal  = [inp.P];   // capital inicial + aportes acumulados (sem juros)
      var dTotal      = [inp.P];   // saldo real (com juros)
      var contribAcum = inp.P;

      for (var i = 0; i < rows.length; i++) {
        contribAcum += rows[i].aporteNoAno;
        labels.push(String(anoAtual + i + 1));
        dPrincipal.push(contribAcum);
        dTotal.push(rows[i].saldoFinal);
      }

      if (chart) {
        chart.data.labels                  = labels;
        chart.data.datasets[0].data        = dPrincipal;
        chart.data.datasets[1].data        = dTotal;
        chart.update();
      }

      // --- Tabela ---
      var tbody = document.getElementById('tabela-body');
      tbody.innerHTML = '';

      for (var j = 0; j < rows.length; j++) {
        var row    = rows[j];
        var isLast = j === rows.length - 1;
        var tr     = document.createElement('tr');
        if (isLast) tr.className = 'highlight';

        function cel(val, bold) {
          return '<td>' + (bold ? '<strong>' + val + '</strong>' : val) + '</td>';
        }

        tr.innerHTML =
          cel(row.ano, isLast) +
          cel(BRL.format(row.saldoInicio), isLast) +
          cel(BRL.format(row.aporteNoAno), isLast) +
          cel(BRL.format(row.juroNoAno), isLast) +
          cel(BRL.format(row.saldoFinal), isLast);

        tbody.appendChild(tr);
      }
    }

    /* ---------- tooltip externo ---------- */
    function customTooltip(context) {
      var chart   = context.chart;
      var tooltip = context.tooltip;

      var el = document.getElementById('calc-tooltip');
      if (!el) {
        el = document.createElement('div');
        el.id = 'calc-tooltip';
        chart.canvas.parentNode.style.position = 'relative';
        chart.canvas.parentNode.appendChild(el);
      }

      if (tooltip.opacity === 0) {
        el.style.opacity = '0';
        return;
      }

      var pts       = tooltip.dataPoints || [];
      var principal = 0, total = 0;
      pts.forEach(function(p) {
        if (p.datasetIndex === 0) principal = p.parsed.y;
        if (p.datasetIndex === 1) total     = p.parsed.y;
      });
      var juros = total - principal;
      var label = tooltip.title && tooltip.title[0] ? tooltip.title[0] : '';
      var titulo = label === 'Agora' ? 'Agora' : 'Ano ' + label;

      el.innerHTML =
        '<div class="ct-title">' + titulo + '</div>' +
        '<div class="ct-row ct-bold"><span>Saldo total</span><span>' + BRL.format(total) + '</span></div>' +
        '<div class="ct-row"><span><span class="ct-dash" style="background:#2d6a9f"></span>Principal total</span><span>' + BRL.format(principal) + '</span></div>' +
        '<div class="ct-row"><span><span class="ct-dash" style="background:#1a3a5c"></span>Juros acumulados</span><span>' + BRL.format(juros) + '</span></div>';

      // Posição relativa ao canvas
      var caret   = tooltip.caretX;
      var canvasW = chart.canvas.offsetWidth;
      var tipW    = 230;
      var left    = caret + 12;
      if (left + tipW > canvasW) left = caret - tipW - 12;

      el.style.opacity    = '1';
      el.style.left       = left + 'px';
      el.style.top        = (tooltip.caretY - 20) + 'px';
    }

    /* ---------- inicializa gráfico ---------- */
    function initChart() {
      var canvas = document.getElementById('grafico');
      if (!canvas || !window.Chart) return;

      chart = new window.Chart(canvas, {
        type: 'line',
        data: {
          labels: [],
          datasets: [
            {
              label: 'Principal total',
              data: [],
              fill: true,
              backgroundColor: 'rgba(45,106,159,0.18)',
              borderColor: 'transparent',
              borderWidth: 0,
              pointRadius: 0,
              pointHoverRadius: 5,
              pointHoverBackgroundColor: '#2d6a9f',
              pointHoverBorderColor: '#fff',
              pointHoverBorderWidth: 2,
              tension: 0.2,
              order: 2,
            },
            {
              label: 'Juros acumulados',
              data: [],
              fill: '-1',
              backgroundColor: 'rgba(26,58,92,0.10)',
              borderColor: '#1a3a5c',
              borderWidth: 2.5,
              pointRadius: 0,
              pointHoverRadius: 5,
              pointHoverBackgroundColor: '#1a3a5c',
              pointHoverBorderColor: '#fff',
              pointHoverBorderWidth: 2,
              tension: 0.2,
              order: 1,
            },
          ],
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          interaction: { mode: 'index', intersect: false },
          plugins: {
            legend: { display: false },
            tooltip: {
              enabled: false,
              external: customTooltip,
            },
          },
          scales: {
            x: {
              grid: { display: false },
              border: { color: '#d1d5db' },
              ticks: {
                color: '#6b7280',
                font: { size: 12 },
                maxTicksLimit: 3,   // só início, meio e fim
                maxRotation: 0,
                autoSkip: true,
              },
            },
            y: {
              min: 0,
              grid: { color: '#f3f4f6' },
              border: { display: false },
              ticks: {
                color: '#6b7280',
                font: { size: 12 },
                maxTicksLimit: 6,
                callback: fmtK,
              },
            },
          },
        },
      });

      atualizar();
    }

    /* ---------- eventos ---------- */
    function debounce(fn) {
      clearTimeout(debounceTimer);
      debounceTimer = setTimeout(fn, 280);
    }

    document.addEventListener('DOMContentLoaded', function () {
      initChart();

      ['inp-inicial', 'inp-anos', 'inp-taxa', 'inp-aporte'].forEach(function (id) {
        var el = document.getElementById(id);
        if (!el) return;
        el.addEventListener('input',  function () { debounce(atualizar); });
        el.addEventListener('change', atualizar);
        el.addEventListener('blur',   atualizar);
      });

      var sel = document.getElementById('inp-freq-cap');
      if (sel) sel.addEventListener('change', atualizar);

      document.querySelectorAll('.calc-toggle').forEach(function (btn) {
        btn.addEventListener('click', function () {
          document.querySelectorAll('.calc-toggle').forEach(function (b) {
            b.classList.remove('active');
          });
          btn.classList.add('active');
          atualizar();
        });
      });
    });

  }());
  </script>

  <style is:global>

    /* ===== HERO ===== */
    .calc-hero {
      background: linear-gradient(to right, #1a3a5c, #2d6a9f);
      padding: 36px 0 44px;
    }
    .calc-hero-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 24px;
    }
    .calc-breadcrumb ol {
      list-style: none;
      padding: 0; margin: 0 0 16px;
      display: flex; flex-wrap: wrap; align-items: center; gap: 6px;
      font-size: 13px;
    }
    .calc-breadcrumb li { display: flex; align-items: center; gap: 6px; margin: 0; }
    .calc-breadcrumb a { color: #bfdbfe; text-decoration: none; }
    .calc-breadcrumb a:hover { text-decoration: underline; }
    .calc-breadcrumb span { color: #bfdbfe; }
    .calc-breadcrumb span.current { color: #fff; font-weight: 500; }
    .calc-hero-title {
      color: #fff;
      font-size: 2.25rem; font-weight: 800; line-height: 1.2;
      margin: 0 0 10px;
    }
    .calc-hero-desc { color: #bfdbfe; font-size: 1.05rem; margin: 0; max-width: 600px; }

    /* ===== MAIN ===== */
    .calc-main {
      max-width: 1200px;
      margin: 0 auto;
      padding: 28px 24px 56px;
    }

    /* ===== TOOL: 2 COLUNAS ===== */
    .calc-tool {
      display: grid;
      grid-template-columns: 340px 1fr;
      grid-template-rows: 1fr;
      gap: 16px;
      margin-bottom: 40px;
    }
    @media (max-width: 860px) {
      .calc-tool { grid-template-columns: 1fr; }
    }

    /* ===== FORM PANEL ===== */
    .calc-form-panel {
      background: #f9fafb;
      padding: 20px 22px;
      border-radius: 0;
      border: 1px solid #e5e7eb;
      display: flex;
      flex-direction: column;
      gap: 14px;
    }
    .calc-form-title {
      font-size: 1rem;
      font-weight: 800;
      color: #111827;
      margin: 0 0 4px;
    }

    /* Fields */
    .calc-field { display: flex; flex-direction: column; gap: 6px; }
    .calc-label {
      font-size: 13px;
      color: #374151;
      font-weight: 500;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    /* Tooltip */
    .tip-wrap {
      position: relative;
      display: inline-flex;
      align-items: center;
      flex-shrink: 0;
    }
    .tip-icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 15px;
      height: 15px;
      border-radius: 50%;
      border: 1.5px solid #9ca3af;
      background: transparent;
      color: #9ca3af;
      font-size: 9px;
      font-weight: 700;
      font-style: normal;
      line-height: 1;
      cursor: default;
      user-select: none;
      flex-shrink: 0;
      transition: border-color 0.15s, color 0.15s;
    }
    .tip-wrap:hover .tip-icon {
      border-color: #2d6a9f;
      color: #2d6a9f;
    }
    .tip-box {
      position: absolute;
      left: 50%;
      bottom: calc(100% + 8px);
      transform: translateX(-50%);
      width: 220px;
      background: #1a3a5c;
      color: #fff;
      font-size: 12px;
      font-weight: 400;
      line-height: 1.5;
      padding: 9px 12px;
      border-radius: 6px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.18);
      pointer-events: none;
      opacity: 0;
      visibility: hidden;
      transition: opacity 0.15s, visibility 0.15s;
      z-index: 50;
      white-space: normal;
      text-align: left;
    }
    /* seta */
    .tip-box::after {
      content: '';
      position: absolute;
      top: 100%;
      left: 50%;
      transform: translateX(-50%);
      border: 5px solid transparent;
      border-top-color: #1a3a5c;
    }
    .tip-wrap:hover .tip-box,
    .tip-wrap:focus-within .tip-box {
      opacity: 1;
      visibility: visible;
    }
    /* primeiros campos: abre para baixo */
    .calc-field:first-child .tip-box,
    .calc-field:nth-child(2) .tip-box {
      top: calc(100% + 8px);
      bottom: auto;
      left: 50%;
      transform: translateX(-50%);
    }
    .calc-field:first-child .tip-box::after,
    .calc-field:nth-child(2) .tip-box::after {
      top: auto;
      bottom: 100%;
      left: 50%;
      transform: translateX(-50%);
      border-top-color: transparent;
      border-bottom-color: #1a3a5c;
    }
    .calc-input-wrap {
      display: flex;
      align-items: center;
      background: #fff;
      border: 1px solid #d1d5db;
      border-radius: 4px;
      overflow: hidden;
      transition: border-color 0.15s, box-shadow 0.15s;
    }
    .calc-input-wrap:focus-within {
      border-color: #2d6a9f;
      box-shadow: 0 0 0 3px rgba(45,106,159,0.12);
    }
    .calc-unit {
      padding: 0 12px;
      font-size: 14px;
      color: #6b7280;
      background: #f9fafb;
      height: 42px;
      display: flex;
      align-items: center;
      white-space: nowrap;
      border-right: 1px solid #e5e7eb;
      flex-shrink: 0;
    }
    .calc-unit.right {
      border-right: none;
      border-left: 1px solid #e5e7eb;
    }
    .calc-input {
      flex: 1;
      border: none;
      outline: none;
      background: transparent;
      padding: 0 12px;
      height: 42px;
      font-size: 15px;
      font-weight: 400;
      color: #374151;
      font-family: Arial, sans-serif;
      min-width: 0;
    }

    /* Select */
    .calc-select-wrap {
      position: relative;
      background: #fff;
      border: 1px solid #d1d5db;
      border-radius: 4px;
      transition: border-color 0.15s;
    }
    .calc-select-wrap:focus-within {
      border-color: #2d6a9f;
      box-shadow: 0 0 0 3px rgba(45,106,159,0.12);
    }
    .calc-select {
      width: 100%;
      border: none;
      outline: none;
      appearance: none;
      -webkit-appearance: none;
      background: transparent;
      padding: 0 36px 0 12px;
      height: 42px;
      font-size: 15px;
      font-weight: 400;
      color: #374151;
      font-family: Arial, sans-serif;
      cursor: pointer;
    }
    .calc-select-arrow {
      position: absolute;
      right: 12px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 10px;
      color: #6b7280;
      pointer-events: none;
    }

    /* Toggle buttons */
    .calc-toggle-group {
      display: flex;
    }
    .calc-toggle {
      flex: 1;
      height: 42px;
      border: 1px solid #d1d5db;
      background: #fff;
      font-size: 14px;
      font-weight: 600;
      color: #374151;
      cursor: pointer;
      transition: background 0.15s, color 0.15s, border-color 0.15s;
      font-family: Arial, sans-serif;
    }
    .calc-toggle:first-child { border-radius: 4px 0 0 4px; border-right: none; }
    .calc-toggle:last-child  { border-radius: 0 4px 4px 0; }
    .calc-toggle.active {
      background: #1a3a5c;
      color: #fff;
      border-color: #1a3a5c;
    }
    .calc-toggle:not(.active):hover { background: #f3f4f6; }

    /* ===== CHART PANEL ===== */
    .calc-chart-panel {
      padding: 20px 24px 16px;
      display: flex;
      flex-direction: column;
      gap: 0;
      height: 100%;
      box-sizing: border-box;
      background: #fff;
      border-radius: 0;
      border: 1px solid #e5e7eb;
      box-shadow: none;
    }
    .calc-chart-header {
      text-align: center;
      margin-bottom: 12px;
    }
    .calc-chart-label {
      font-size: 12px;
      color: #6b7280;
      margin: 0 0 2px;
      font-weight: 500;
    }
    .calc-chart-total {
      font-size: 2rem;
      font-weight: 900;
      color: #1a3a5c;
      margin: 0;
      letter-spacing: -0.02em;
      line-height: 1;
    }
    .calc-chart-area {
      flex: 1;
      position: relative;
      min-height: 0;
    }
    .calc-chart-legend {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 10px;
      font-size: 12px;
      color: #374151;
    }
    .calc-legend-item {
      display: flex;
      align-items: center;
      gap: 7px;
    }
    .calc-legend-swatch {
      width: 14px;
      height: 14px;
      border-radius: 3px;
      display: inline-block;
      flex-shrink: 0;
    }
    .calc-legend-swatch.principal { background: rgba(45,106,159,0.35); border: 1.5px solid rgba(45,106,159,0.6); }
    .calc-legend-swatch.juros     { background: rgba(26,58,92,0.25); border: 2px solid #1a3a5c; }

    /* ===== TABLE ===== */
    .calc-table-section {
      background: #fff;
      border: 1px solid #e5e7eb;
      border-radius: 12px;
      padding: 28px;
      margin-bottom: 32px;
    }
    .calc-section-title {
      font-size: 1.05rem;
      font-weight: 700;
      color: #111827;
      margin: 0 0 20px;
    }
    .calc-table-scroll { overflow-x: auto; }
    .calc-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 14px;
    }
    .calc-table th {
      background: #f9fafb;
      padding: 10px 16px;
      text-align: right;
      font-size: 12px;
      font-weight: 700;
      color: #6b7280;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      white-space: nowrap;
      border-bottom: 1px solid #e5e7eb;
    }
    .calc-table th:first-child { text-align: center; }
    .calc-table td {
      padding: 11px 16px;
      text-align: right;
      color: #374151;
      border-bottom: 1px solid #f3f4f6;
      font-size: 14px;
      white-space: nowrap;
    }
    .calc-table td:first-child { text-align: center; color: #9ca3af; font-weight: 600; }
    .calc-table tbody tr:hover { background: #f9fafb; }
    .calc-table tr.highlight td {
      background: #eff6ff;
      color: #1a3a5c;
      font-weight: 700;
      border-bottom: none;
    }
    .calc-table tr.highlight td:first-child { color: #1a3a5c; }

    /* ===== INFO ===== */
    .calc-info {
      background: #fff;
      border: 1px solid #e5e7eb;
      border-radius: 12px;
      padding: 32px;
    }
    .calc-info-text h2 {
      font-size: 1.2rem;
      font-weight: 800;
      color: #111827;
      margin: 0 0 10px;
    }
    .calc-info-text h3 {
      font-size: 1rem;
      font-weight: 700;
      color: #1a3a5c;
      margin: 24px 0 8px;
    }
    .calc-info-text p {
      font-size: 15px;
      color: #374151;
      line-height: 1.7;
      margin: 0 0 12px;
    }
    .calc-formula-block {
      background: #f9fafb;
      border-left: 3px solid #2d6a9f;
      border-radius: 0 6px 6px 0;
      padding: 14px 18px;
      margin-bottom: 16px;
    }
    .calc-formula-block code {
      font-family: 'Courier New', monospace;
      font-size: 15px;
      font-weight: 700;
      color: #1a3a5c;
      background: none;
      padding: 0;
    }
    .calc-formula-vars {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(210px, 1fr));
      gap: 6px 16px;
      font-size: 13px;
      color: #4b5563;
    }
    .calc-formula-vars strong { color: #1a3a5c; }

    /* ===== TOOLTIP EXTERNO ===== */
    #calc-tooltip {
      position: absolute;
      pointer-events: none;
      opacity: 0;
      transition: opacity 0.15s;
      background: #fff;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.10);
      padding: 12px 14px;
      min-width: 210px;
      z-index: 20;
      font-family: Arial, sans-serif;
    }
    .ct-title {
      font-size: 12px;
      color: #6b7280;
      font-weight: 500;
      margin-bottom: 8px;
    }
    .ct-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 16px;
      font-size: 13px;
      color: #374151;
      padding: 3px 0;
    }
    .ct-row span:first-child {
      display: flex;
      align-items: center;
      gap: 7px;
      white-space: nowrap;
    }
    .ct-row span:last-child {
      font-variant-numeric: tabular-nums;
      white-space: nowrap;
    }
    .ct-bold {
      font-weight: 700;
      color: #111827;
      font-size: 14px;
      border-bottom: 1px solid #f3f4f6;
      padding-bottom: 8px;
      margin-bottom: 4px;
    }
    .ct-dash {
      display: inline-block;
      width: 14px;
      height: 2.5px;
      border-radius: 2px;
      flex-shrink: 0;
    }

    /* ===== MOBILE ===== */
    @media (max-width: 768px) {
      .calc-hero-title { font-size: 1.6rem; }
      .calc-main { padding: 20px 16px 48px; }
      .calc-chart-total { font-size: 1.6rem; }
      .calc-chart-area { min-height: 180px; }
      .calc-table-section, .calc-info { padding: 20px 16px; }
    }
  </style>

</body>
</html>
